# Optional. When set, demo/contact form submissions are POSTed here as JSON
# (Slack incoming webhooks are detected and formatted automatically).
# Without it, submissions are logged on the server and still acknowledged.
DEMO_WEBHOOK_URL=
